---
- name: Setup systemd service
  template:
    src: vm.service.j2
    dest: /etc/systemd/system/victoriametrics.service
  notify:
    - reload systemd
    - restart vm

- name: Ensure VictoriaMetrics service is enabled and started
  systemd:
    name: victoriametrics
    state: started
    enabled: yes
