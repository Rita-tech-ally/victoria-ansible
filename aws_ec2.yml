plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1
filters:
  instance-state-name: running
# Remove hostnames if it causes issues
compose:
  ansible_host: public_ip_address
keyed_groups:
  - key: tags.Name
    prefix: tag
    separator: "_"
strict: False
