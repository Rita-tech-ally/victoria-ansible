plugin: amazon.aws.aws_ec2

regions:
  - ap-south-1

filters:
  instance-state-name: running

hostnames:
  - public_ip_address   # <-- previously you had "ip-address", must be this

# Optional: group instances by tags
keyed_groups:
  - key: tags.Name
    prefix: tag
