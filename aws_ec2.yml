plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1
filters:
  instance-state-name: running
hostnames:
  - public_ip_address   # must match what you connect via (public/private)
compose:
  ansible_host: public_ip_address
keyed_groups:
  - key: tags.Name
    prefix: tag
